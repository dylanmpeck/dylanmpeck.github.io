import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
      background: {
        default: '#1B1D21',
        paper: '#212229',
      },
      text: {
        primary: '#999',
        secondary: '#0b90ce',
      }
    }
  })